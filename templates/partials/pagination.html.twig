{% if base_url == '' %}
    {% set base_url = '/' %}
{% endif %}

<nav class="pagination" role="navigation">
    {% if pagination.hasNext %}
        {% set url = (base_url ~ pagination.params ~ pagination.nextUrl)|replace({'//':'/'}) %}
        <a class="older-posts" href="{{ url }}">&larr; {{ 'THEME_HEREDITOR.OLDER' | t }}</a>
    {% endif %}
    <span class="page-number">Page {{ uri.currentPage }} / {{ pagination|length }}</span>
    {% if pagination.hasPrev %}
        {% set url =  (base_url ~ pagination.params ~ pagination.prevUrl)|replace({'//':'/'}) %}
        <a class="newer-posts" href="{{ url }}">{{ 'THEME_HEREDITOR.NEWER' | t }} &rarr;</a>
    {% endif %}
</nav>
